//! Snapshot serialization (feature-gated).
//! Provides `SimSnapshot` and related types for network transfer / saving.

// Snapshot types are defined inline in `simulation.rs`
// This module exists to satisfy the `pub mod snapshot` declaration.
